(()=>{
'use strict';
const K='emtidad_family_tree_v2',$=id=>document.getElementById(id),uid=()=>Date.now().toString(36)+Math.random().toString(36).slice(2,8),esc=s=>String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
let people=[];try{let raw=JSON.parse(localStorage.getItem(K)||'null');if(!Array.isArray(raw)){const legacy=JSON.parse(localStorage.getItem('emtidad_family_tree_v1')||'null');if(Array.isArray(legacy))raw=legacy;}if(Array.isArray(raw)){people=raw.filter(p=>p&&p.id&&p.name).map(p=>({id:String(p.id),name:String(p.name),gender:p.gender==='female'?'female':'male',birthYear:String(p.birthYear||p.birth||''),deathYear:String(p.deathYear||p.death||''),fatherId:String(p.fatherId||p.father||''),motherId:String(p.motherId||p.mother||''),branch:String(p.branch||''),notes:String(p.notes||''),spouseIds:Array.isArray(p.spouseIds)?p.spouseIds.map(String):[]}));if(!localStorage.getItem(K)&&people.length){localStorage.setItem(K,JSON.stringify(people));localStorage.setItem(K+'_updated',new Date().toISOString());}}}catch{}
const get=id=>people.find(p=>p.id===id),childrenOf=id=>people.filter(p=>p.fatherId===id||p.motherId===id),spouseNames=p=>(p.spouseIds||[]).map(get).filter(Boolean).map(x=>x.name);
function ancestors(id,seen=new Set()){if(!id||seen.has(id))return seen;seen.add(id);const p=get(id);if(!p)return seen;if(p.fatherId)ancestors(p.fatherId,seen);if(p.motherId)ancestors(p.motherId,seen);return seen;}
function wouldCycle(personId,parentId){if(!personId||!parentId)return false;return personId===parentId||ancestors(parentId).has(personId);}
function generation(p,seen=new Set()){if(!p||seen.has(p.id))return 0;const next=new Set(seen);next.add(p.id);const parents=[get(p.fatherId),get(p.motherId)].filter(Boolean);if(parents.length)return 1+Math.max(...parents.map(x=>generation(x,next)));const spouse=(p.spouseIds||[]).map(get).find(x=>x&&(x.fatherId||x.motherId));return spouse?generation(spouse,next):0;}
function save(){localStorage.setItem(K,JSON.stringify(people));localStorage.setItem(K+'_updated',new Date().toISOString());render();}
function cardHtml(p){const s=spouseNames(p),childCount=childrenOf(p.id).length,years=p.birthYear&&p.deathYear?`${p.birthYear} – ${p.deathYear}`:p.birthYear?`مواليد ${p.birthYear}`:p.deathYear?`توفي ${p.deathYear}`:'';return `<div class="ft-head"><span class="ft-avatar ${p.gender==='female'?'f':''}">${esc((p.name||'?').charAt(0))}</span><div class="ft-name"><b>${esc(p.name)}</b><span>${esc(years)}</span></div></div><div class="ft-tags">${s.length?`<span>زوج: ${esc(s.join('، '))}</span>`:''}${childCount?`<span>${childCount} من الأبناء</span>`:''}${p.branch?`<span>${esc(p.branch)}</span>`:''}</div>`;}
function render(){const gens=$('gens'),empty=$('empty'),svg=$('lines');gens.innerHTML='';svg.innerHTML='';if(!people.length){empty.hidden=false;$('count').textContent=$('genCount').textContent='0';$('updated').textContent='—';return}empty.hidden=true;const values=people.map(generation),max=Math.max(...values),map=new Map;people.forEach((p,i)=>{const g=values[i];if(!map.has(g))map.set(g,[]);map.get(g).push(p)});for(let g=0;g<=max;g++){const row=document.createElement('div');row.className='ft-gen';row.innerHTML=`<small>الجيل ${g+1}</small>`;(map.get(g)||[]).sort((a,b)=>a.name.localeCompare(b.name,'ar')).forEach(p=>{const card=document.createElement('article');card.className='ft-person';card.dataset.id=p.id;card.tabIndex=0;card.innerHTML=cardHtml(p);card.addEventListener('click',()=>openModal(p.id));card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openModal(p.id)}});row.appendChild(card)});gens.appendChild(row)}$('count').textContent=people.length;$('genCount').textContent=max+1;const u=localStorage.getItem(K+'_updated');$('updated').textContent=u?new Intl.DateTimeFormat('ar-SA',{dateStyle:'medium'}).format(new Date(u)):'—';requestAnimationFrame(drawLines);applySearch();}
function center(el){const r=el.getBoundingClientRect(),c=$('canvas').getBoundingClientRect();return {x:r.left-c.left+r.width/2,y:r.top-c.top+r.height/2,top:r.top-c.top,bottom:r.bottom-c.top,left:r.left-c.left,right:r.right-c.left};}
function path(a,b,cls=''){const p=document.createElementNS('http://www.w3.org/2000/svg','path'),mid=(a.y+b.y)/2;p.setAttribute('d',`M ${a.x} ${a.y} C ${a.x} ${mid}, ${b.x} ${mid}, ${b.x} ${b.y}`);if(cls)p.setAttribute('class',cls);$('lines').appendChild(p);}
function drawLines(){const svg=$('lines'),canvas=$('canvas');svg.innerHTML='';svg.setAttribute('viewBox',`0 0 ${canvas.scrollWidth} ${canvas.scrollHeight}`);const seenSpouse=new Set;people.forEach(p=>{const child=document.querySelector(`.ft-person[data-id="${CSS.escape(p.id)}"]`);if(!child)return;[p.fatherId,p.motherId].filter(Boolean).forEach(pid=>{const par=document.querySelector(`.ft-person[data-id="${CSS.escape(pid)}"]`);if(par){const a=center(par),b=center(child);path({x:a.x,y:a.bottom},{x:b.x,y:b.top});}});(p.spouseIds||[]).forEach(sid=>{const key=[p.id,sid].sort().join('|');if(seenSpouse.has(key))return;seenSpouse.add(key);const spouse=document.querySelector(`.ft-person[data-id="${CSS.escape(sid)}"]`);if(spouse){const a=center(child),b=center(spouse),y=(a.y+b.y)/2;const line=document.createElementNS('http://www.w3.org/2000/svg','path');line.setAttribute('d',`M ${a.x} ${y} L ${b.x} ${y}`);line.setAttribute('class','spouse');svg.appendChild(line);}})});}
function optionList(gender,currentId){return people.filter(p=>p.id!==currentId&&(!gender||p.gender===gender)).sort((a,b)=>a.name.localeCompare(b.name,'ar')).map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');}
function selectedValues(select){return [...select.selectedOptions].map(o=>o.value).filter(Boolean);}
function fillRelations(currentId=''){const current=get(currentId);$('father').innerHTML='<option value="">—</option>'+optionList('male',currentId);$('mother').innerHTML='<option value="">—</option>'+optionList('female',currentId);$('spouses').innerHTML=people.filter(p=>p.id!==currentId).sort((a,b)=>a.name.localeCompare(b.name,'ar')).map(p=>`<option value="${p.id}">${esc(p.name)}</option>`).join('');if(current){$('father').value=current.fatherId||'';$('mother').value=current.motherId||'';const ids=new Set(current.spouseIds||[]);[...$('spouses').options].forEach(o=>o.selected=ids.has(o.value));}}
function openModal(id=''){const p=get(id);$('modalTitle').textContent=p?'تعديل فرد':'إضافة فرد';$('id').value=p?.id||'';$('name').value=p?.name||'';$('gender').value=p?.gender||'male';$('birth').value=p?.birthYear||'';$('death').value=p?.deathYear||'';$('branch').value=p?.branch||'';$('notes').value=p?.notes||'';fillRelations(id);$('deleteBtn').hidden=!p;$('quickChildBtn').hidden=!p;$('modal').hidden=false;setTimeout(()=>$('name').focus(),30);}
function closeModal(){$('modal').hidden=true;}
function syncSpouses(personId,newIds){const person=get(personId);if(!person)return;const old=new Set(person.spouseIds||[]),next=new Set(newIds);people.forEach(p=>{if(p.id===personId)return;const ids=new Set(p.spouseIds||[]);if(next.has(p.id))ids.add(personId);else if(old.has(p.id))ids.delete(personId);p.spouseIds=[...ids];});person.spouseIds=[...next];}
function submit(e){e.preventDefault();const id=$('id').value||uid(),fatherId=$('father').value,motherId=$('mother').value;if(fatherId&&motherId&&fatherId===motherId){alert('الأب والأم يجب أن يكونا شخصين مختلفين');return}if(wouldCycle(id,fatherId)||wouldCycle(id,motherId)){alert('هذه العلاقة ستنشئ دورة داخل الشجرة. راجع اختيار الأب أو الأم.');return}let p=get(id);if(!p){p={id,spouseIds:[]};people.push(p)}p.name=$('name').value.trim();p.gender=$('gender').value;p.birthYear=$('birth').value.trim();p.deathYear=$('death').value.trim();p.fatherId=fatherId;p.motherId=motherId;p.branch=$('branch').value.trim();p.notes=$('notes').value.trim();syncSpouses(id,selectedValues($('spouses')));save();closeModal();}
function removePerson(){const id=$('id').value,p=get(id);if(!p||!confirm(`حذف ${p.name} من الشجرة؟`))return;people=people.filter(x=>x.id!==id).map(x=>({...x,fatherId:x.fatherId===id?'':x.fatherId,motherId:x.motherId===id?'':x.motherId,spouseIds:(x.spouseIds||[]).filter(s=>s!==id)}));save();closeModal();}
function quickChild(){const parent=get($('id').value);if(!parent)return;closeModal();openModal();if(parent.gender==='male')$('father').value=parent.id;else $('mother').value=parent.id;}
function applySearch(){const q=($('search').value||'').trim().toLowerCase();document.querySelectorAll('.ft-person').forEach(el=>{const p=get(el.dataset.id),hay=[p?.name,p?.branch,p?.notes].join(' ').toLowerCase();el.classList.toggle('hit',Boolean(q&&hay.includes(q)));});}
function sample(){if(people.length&&!confirm('سيستبدل المثال الشجرة الحالية. متابعة؟'))return;const a={id:uid(),name:'عبدالله',gender:'male',birthYear:'1948',deathYear:'',fatherId:'',motherId:'',branch:'الجيل المؤسس',notes:'',spouseIds:[]},b={id:uid(),name:'نورة',gender:'female',birthYear:'1953',deathYear:'',fatherId:'',motherId:'',branch:'',notes:'',spouseIds:[a.id]};a.spouseIds=[b.id];const c={id:uid(),name:'سعد',gender:'male',birthYear:'1975',deathYear:'',fatherId:a.id,motherId:b.id,branch:'فرع سعد',notes:'',spouseIds:[]},d={id:uid(),name:'خالد',gender:'male',birthYear:'1979',deathYear:'',fatherId:a.id,motherId:b.id,branch:'فرع خالد',notes:'',spouseIds:[]},e={id:uid(),name:'ريم',gender:'female',birthYear:'1982',deathYear:'',fatherId:a.id,motherId:b.id,branch:'',notes:'',spouseIds:[]},f={id:uid(),name:'فيصل',gender:'male',birthYear:'2002',deathYear:'',fatherId:c.id,motherId:'',branch:'فرع سعد',notes:'',spouseIds:[]},g={id:uid(),name:'سارة',gender:'female',birthYear:'2005',deathYear:'',fatherId:c.id,motherId:'',branch:'فرع سعد',notes:'',spouseIds:[]};people=[a,b,c,d,e,f,g];save();}
function download(name,type,text){const blob=new Blob([text],{type}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1200);}
function exportJson(){download(`emtidad-family-tree-${new Date().toISOString().slice(0,10)}.json`,'application/json;charset=utf-8',JSON.stringify({version:2,exportedAt:new Date().toISOString(),people},null,2));}
function gedDate(year){return year?`1 JAN ${year}`:'';}
function exportGedcom(){const map=new Map(people.map((p,i)=>[p.id,`@I${i+1}@`])),families=[],seen=new Set;people.forEach(p=>(p.spouseIds||[]).forEach(sid=>{const key=[p.id,sid].sort().join('|');if(seen.has(key))return;seen.add(key);const spouse=get(sid);if(!spouse)return;const husband=p.gender==='male'?p:spouse.gender==='male'?spouse:null,wife=p.gender==='female'?p:spouse.gender==='female'?spouse:null;let children=[];if(husband&&wife)children=people.filter(c=>c.fatherId===husband.id&&c.motherId===wife.id);else if(husband)children=people.filter(c=>c.fatherId===husband.id);else if(wife)children=people.filter(c=>c.motherId===wife.id);families.push({husband,wife,children});}));const lines=['0 HEAD','1 GEDC','2 VERS 7.0','1 CHAR UTF-8','1 SOUR EMTIDAD'];people.forEach(p=>{lines.push(`0 ${map.get(p.id)} INDI`,`1 NAME ${p.name.replace(/\//g,' ')}`,`1 SEX ${p.gender==='male'?'M':'F'}`);if(p.birthYear)lines.push('1 BIRT',`2 DATE ${gedDate(p.birthYear)}`);if(p.deathYear)lines.push('1 DEAT',`2 DATE ${gedDate(p.deathYear)}`);if(p.notes)lines.push(`1 NOTE ${p.notes.replace(/\n/g,' ')}`);});families.forEach((f,i)=>{const id=`@F${i+1}@`;lines.push(`0 ${id} FAM`);if(f.husband)lines.push(`1 HUSB ${map.get(f.husband.id)}`);if(f.wife)lines.push(`1 WIFE ${map.get(f.wife.id)}`);f.children.forEach(c=>lines.push(`1 CHIL ${map.get(c.id)}`));});lines.push('0 TRLR');download(`emtidad-family-tree-${new Date().toISOString().slice(0,10)}.ged`,'text/plain;charset=utf-8',lines.join('\n'));}
function importJson(file){const r=new FileReader;r.onload=()=>{try{const data=JSON.parse(r.result),next=Array.isArray(data)?data:data.people;if(!Array.isArray(next))throw new Error;if(people.length&&!confirm('سيستبدل الاستيراد الشجرة الحالية. متابعة؟'))return;people=next.filter(p=>p&&p.id&&p.name).map(p=>({id:String(p.id),name:String(p.name),gender:p.gender==='female'?'female':'male',birthYear:String(p.birthYear||p.birth||''),deathYear:String(p.deathYear||p.death||''),fatherId:String(p.fatherId||p.father||''),motherId:String(p.motherId||p.mother||''),branch:String(p.branch||''),notes:String(p.notes||''),spouseIds:Array.isArray(p.spouseIds)?p.spouseIds.map(String):[]}));save();}catch{alert('تعذر قراءة ملف الشجرة')}};r.readAsText(file);}
$('addBtn').addEventListener('click',()=>openModal());$('firstBtn').addEventListener('click',()=>openModal());$('sampleBtn').addEventListener('click',sample);$('printBtn').addEventListener('click',()=>window.print());$('exportBtn').addEventListener('click',exportJson);$('gedcomBtn').addEventListener('click',exportGedcom);$('importFile').addEventListener('change',e=>{if(e.target.files[0])importJson(e.target.files[0]);e.target.value=''});$('search').addEventListener('input',applySearch);$('form').addEventListener('submit',submit);$('deleteBtn').addEventListener('click',removePerson);$('quickChildBtn').addEventListener('click',quickChild);$('closeBtn').addEventListener('click',closeModal);$('cancelBtn').addEventListener('click',closeModal);$('modal').addEventListener('click',e=>{if(e.target===$('modal'))closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!$('modal').hidden)closeModal()});window.addEventListener('resize',()=>requestAnimationFrame(drawLines));render();
})();